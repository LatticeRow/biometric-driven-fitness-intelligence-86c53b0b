import Foundation

struct ImportCoordinator {
    let supportedSource = "Google Fit export"

    let metricNames = [
        "Sleep",
        "Resting heart rate",
        "Body weight",
    ]

    var summary: String {
        "Imports from Files land here next."
    }
}
